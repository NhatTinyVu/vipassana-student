import {
  get,
  setDisable,
  enable,
  disable,
  handler,
  setHandler,
  resetHandler,
  resetDisable,
  reset,
} from './back-press';

export default {
  get,
  setDisable,
  enable,
  disable,
  handler,
  setHandler,
  resetHandler,
  resetDisable,
  reset,
};
